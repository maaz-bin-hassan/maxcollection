// API configuration
const API_BASE_URL = 'https://maxcollection-backend.onrender.com';

export const API_ENDPOINTS = {
  products: `${API_BASE_URL}/api/products`,
  orders: `${API_BASE_URL}/api/orders`,
};

export default API_BASE_URL;